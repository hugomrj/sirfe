

window.onload = function() {
    
    //menu_principal();
    html.menu.mostrar();
    
    
/*
    ajax.url = pathAbs()+'/Menu';
    ajax.metodo = "GET";
    document.getElementById( 'menubar').innerHTML =  ajax.private.ws();
    redirect(ajax.state);
*/ 
    /*
           ajax.url = pathAbs()+"/api/usuarios/login";           
           ajax.post.json( jsonDataForm("form_login")  );          
    */
          


};









/*
window.onresize = function() {
    
    var nodeList = document.querySelectorAll('.fondo_oscuro');
    for (var i = 0; i < nodeList.length; ++i) {
        document.getElementById(nodeList[i].id).style.height = document.body.scrollHeight;        
    }

};

*/

